import { MantineStyleProp } from '@mantine/core';

export const stylesUnbanUser = {
  banReason: (): MantineStyleProp => ({
    maxWidth: 260,
    whiteSpace: 'pre-wrap',
    wordBreak: 'break-word',
  }),
};
