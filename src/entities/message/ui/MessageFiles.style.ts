import { MantineStyleProp } from '@mantine/core';

export const messageFilesStyles = {
  message: (): MantineStyleProp => ({
    background: 'rgba(255,255,255,0.1)',
    padding: '4px 8px',
    borderRadius: 6,
    cursor: 'pointer',
  }),
};
